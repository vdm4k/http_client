{
    "version": "0.2.0",
    "configurations": [        
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/build/examples/simple_client/http_simple_client",
            "args": ["--url", "https://mobile-review.com/"],
            "cwd": "${workspaceFolder}"
        }
    ]
}